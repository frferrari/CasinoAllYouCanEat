import org.scalatest.{Matchers, WordSpec}

class StateTest extends WordSpec with Matchers {
  "A State with totalPayed=10" when {
    "checked for available seats and having an empty list of eating guests" should {
      "return true " in {
      }
    }
  }
}
